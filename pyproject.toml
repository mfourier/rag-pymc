[build-system]
requires = ["setuptools>=77,<81"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-pymc"
version = "0.1.0"
description = "An evidence-grounded expert assistant for Bayesian statistics and PyMC"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "beautifulsoup4==4.15.0",
  "mcp==2.0.0",
  "pydantic==2.13.4",
  "typer==0.27.0",
]

[project.optional-dependencies]
scientific = [
  "arviz==1.2.0",
  "h5netcdf==1.8.1",
  "h5py==3.16.0",
  "pymc==6.2.0",
  "pytensor==3.2.2",
  "xarray==2026.2.0",
]

[project.scripts]
rag-pymc = "rag_pymc.cli:app"

[dependency-groups]
dev = [
  "mypy==2.3.0",
  "pre-commit==4.6.0",
  "pytest==9.1.1",
  "pytest-cov==7.1.0",
  "ruff==0.15.22",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
rag_pymc = ["py.typed"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = [
  "datasets/fixtures/pymc/*/notebooks/**",
  "datasets/fixtures/pymc/*/repository/**",
]
force-exclude = true

[tool.ruff.lint]
select = ["ANN", "B", "C90", "D", "E", "F", "I", "RUF", "SIM", "UP"]

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
pretty = true
files = ["src", "tests", "tools", ".agents/skills"]

[tool.pytest.ini_options]
addopts = [
  "-ra",
  "--strict-config",
  "--strict-markers",
]
filterwarnings = ["error"]
testpaths = ["tests"]

[tool.coverage.run]
source = ["rag_pymc"]
branch = true

[tool.coverage.report]
fail_under = 84
exclude_also = [
  "if TYPE_CHECKING:",
  "if __name__ == .__main__.:",
]
show_missing = true
skip_covered = true
